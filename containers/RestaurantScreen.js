import { Text, View } from "react-native";

const RestaurantScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>This is the RestaurantScreen component</Text>
    </View>
  );
};

export default RestaurantScreen;
